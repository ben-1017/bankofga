<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.4</version>
        <relativePath/>
    </parent>

    <groupId>com.bankofgeorgia</groupId>
    <artifactId>bank-of-georgia-backend</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>bank-of-georgia-backend</name>
    <description>Parent POM for Bank of Georgia microservices</description>

    <properties>
        <java.version>17</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <spring-cloud.version>2023.0.3</spring-cloud.version>
        <!-- Spring Boot 3.3.4 ships Mockito 5.11, whose bundled ByteBuddy can't
             read JDK 24 class files. Bump Mockito (and ByteBuddy) to versions
             that support JDK 24 so unit tests run on a modern JDK. -->
        <mockito.version>5.15.2</mockito.version>
        <byte-buddy.version>1.15.11</byte-buddy.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <modules>
        <module>customer-service</module>
        <module>product-service</module>
        <module>notification-service</module>
        <module>account-service</module>
        <module>transaction-service</module>
        <module>scheduler-service</module>
        <module>api-gateway</module>
    </modules>
</project>
