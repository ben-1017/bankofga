{
  "name": "bankofga-integration-tests",
  "private": true,
  "version": "0.0.0",
  "description": "Newman runner for the BankOfGA Postman collection. Exercises all 5 services end-to-end.",
  "scripts": {
    "test": "newman run ../../docs/BankOfGA.postman_collection.json --reporters cli,junit --reporter-junit-export ./reports/junit.xml --bail folder",
    "test:no-bail": "newman run ../../docs/BankOfGA.postman_collection.json --reporters cli,junit --reporter-junit-export ./reports/junit.xml",
    "wait-for-services": "./wait-for-services.sh",
    "ci": "./wait-for-services.sh && npm test"
  },
  "devDependencies": {
    "newman": "^6.2.1"
  }
}
